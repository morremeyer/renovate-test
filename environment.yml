channels:
  - conda-forge
  - defaults
dependencies:
  # click has 7.1.1, 7.1.2, 8.0.[0-1,3-4] in main/click
  - click=7.1
  # dask is 2022.7.0 in main/dask
  - dask
  # distributed is 2022.7.0 in main/distributed
  - distributed=2022.5.0
  # pip 22.1.2 is up to date in main/pip
  - pip=22.1.2
  - pip:
    - vyper-config
